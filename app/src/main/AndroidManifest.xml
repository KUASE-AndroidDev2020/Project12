<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessmotivation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YoutubeBC"></activity>
        <activity android:name=".YoutubeSLL" />
        <activity android:name=".YoutubePU" />
        <activity android:name=".SaveActivity" />
        <activity android:name=".OptionActivity" />
        <activity android:name=".WorkoutActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".YoutubeBP" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".RecordActivity" />
        <activity android:name=".CalenderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>